touch.gitignore
git config --global user.name Attadeep
git config --global user.email "attadeepj@gmail.com"
git init
git add .
git commit -m "Initial Commit"
git status #just to see
git remote add origin master